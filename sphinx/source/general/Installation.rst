Installation
====================

|PackageName| is easy to install from PYPI using the standard python3 **pip** command.

.. code::

    $ pip install femedu

This will install the actual package in your :code:`site-packages` folder

You should be ready to go.

Requirements
-------------

* :code:`numpy`
* :code:`scipy`
* :code:`matplotlib`
